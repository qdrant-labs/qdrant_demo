{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "sh -c 'python -m pip uninstall -y fastembed; exec python -m uvicorn qdrant_demo.service:app --host 0.0.0.0 --port ${PORT:-8000}'"
  }
}
