{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "python -m pip uninstall -y fastembed || true; python -m uvicorn qdrant_demo.service:app --host 0.0.0.0 --port ${PORT:-8000}"
  }
}
