{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -m pip uninstall -y fastembed || true; python -m uvicorn qdrant_demo.service:app --host 0.0.0.0 --port ${PORT:-8000} --workers ${WORKERS:-1}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
